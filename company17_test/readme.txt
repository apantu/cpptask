«адача:
— использованием исключительно C++ и STL разработайте class, реализующий хранилище товаров.
’ранилище целиком хранитс€ в оперативной пам€ти.
«апись хранилища содержит пол€:
уникальный артикул Ц строка
производитель товара Ц строка
наименование товара Ц строка
цена Ц целое число
Ќеобходимо реализовать операции:
добавить товар в хранилище. —ложность O(log(N)), N Ц текущий размер хранилища
получить товар с заданным артикулом. —ложность в среднем случае Ц O(1), сложность в худшем
случае Ц O(N)
получить множество товаров заданного производител€. —ложность в среднем случае Ц O(log(N)),
сложность в худшем случае Ц O(N)
удалить товар с заданным артикулом. —ложность в среднем случае Ц O(log(N)), сложность в
худшем случае Ц O(N)
ѕеречисленные операции должны быть разработаны с учетом возможности их вызова в
многопоточной среде.